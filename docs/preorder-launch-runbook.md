# Frame pre-order launch runbook pointer

The canonical operational procedure is
[`../PREORDER_LAUNCH_RUNBOOK.md`](../PREORDER_LAUNCH_RUNBOOK.md).

The evidence, approval and incorporation handoff template is
[`preorder-pre-incorporation-pack.md`](preorder-pre-incorporation-pack.md).

This file intentionally contains no duplicate launch instructions. Update the
canonical runbook so the private smoke test, public cutover and emergency-pause
procedure cannot drift between documents.
